/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // Ensures React runs in strict mode for better error detection
    output: "standalone",  // Enables standalone mode for deployments
  };
  
  export default nextConfig;